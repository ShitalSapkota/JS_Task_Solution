let a = 20;
let b = 2;

add = a + b;  // adding two variables value
diff = a - b; // substracting variables value
pro = a * b;  // multiplying 
quo = a / b;  // dividing
rem = a % b;  // reminders

console.log(add, diff, pro, quo, rem);
